# Assignment 1 & 2

install.packages('swirl')
library(swirl)
swirl()

# can make matrices quickly by creating a vector of the values, then setting the dimensions with dim() which will stick all the values into the prescribed rows/columns.

